- if org.key?(field)
  %section{:class=>css_class_format(field.to_s)}
    - if defined? icon
      %i{:class=>icon}
    %h1 #{stitle}:
    %ul
      - org[field].sort.each do |access|
        %li
          %span
            = access