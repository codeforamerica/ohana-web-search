- if org.key?(field)
  %section{:class=>css_class_format(field.to_s)}
    - if defined? iconclass
      %i{:class=>iconclass}
    %h1 #{stitle}:
    %ul
      - org[field].sort.each do |lang|
        %li
          %span
            -# Disables languages link till there's a corresponding filter that can be toggled!
              - lang_url = /.+\((.+)\)/.match(lang).nil? ? lang : /.+\((.+)\)/.match(lang)[1]
              = link_to lang, organizations_path(request.query_parameters.merge(language: lang))
            = lang