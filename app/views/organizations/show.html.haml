- title @org.name
- @org.name == @org.organization.name ? canonical("#{organizations_url}/#{@org.slugs[-1]}") : canonical("#{organizations_url}/#{@org.organization.slugs[-1]}/#{@org.slugs[-1]}")
= render 'component/search/aside'
%section#results-container
  = render 'component/organizations/detail/body'