- title @location.name
- @location.name == @location.organization.name ? canonical("#{locations_url}/#{@location.slugs.last}") : canonical("#{locations_url}/#{@location.organization.slugs.last}/#{@location.slugs.last}")
= render 'component/search/aside'
%section#results-container
  = render 'component/locations/detail/body'