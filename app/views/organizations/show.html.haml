- title @org.name
- @org.name == @org.organization.name ? canonical("#{organizations_url}/#{@org.slug}") : canonical("#{organizations_url}/#{@org.organization.slug}/#{@org.slug}")
= render 'component/search/aside'
%section#results-container
  = render 'component/organizations/detail/body'