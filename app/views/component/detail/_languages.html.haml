- if location[:languages].present?
  %section{:class=>css_class_format('languages')}
    - if defined? iconclass
      %i{:class=>iconclass}
    %h1 Languages Spoken:
    %ul
      - location.languages.sort.each do |lang|
        %li
          %span
            -# Disables languages link till there's a corresponding filter that can be toggled!
              = link_to lang, locations_path(request.query_parameters.merge(language: lang))
            = lang